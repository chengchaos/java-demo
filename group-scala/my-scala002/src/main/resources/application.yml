
server:
  port: 8080
spring:
  application:
    name: my-scala002
  cloud:
    service-registry:
      auto-registration:
        enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
